<script>
    PubSub.on('user', (data) => {
        !data.school_reviewer.has_admin && !data.school_reviewer.young_associate_department_permissions.length && window.history.back();
    });

    window.getSystem = () => 1;
</script>
<%- include ('studentData') %>
